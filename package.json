{
  "name": "paninotl-dashboard",
  "version": "2.0.0",
  "private": true,
  "description": "PaninoTL static dashboard with optional local MQTT-over-HTTP bridge",
  "scripts": {
    "prestart": "npm install",
    "start": "node local/server.js",
    "prelocal": "npm install",
    "local": "node local/server.js"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.0",
    "mqtt": "^5.10.4"
  }
}
